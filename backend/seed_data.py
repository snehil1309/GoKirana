import models


def seed_database(db):
    # No pre-seeded data — all accounts are created from scratch via the app.
    return {"message": "No seed data. Create accounts via the registration flow."}
